# pwc-paper-list 环境配置 / Environment configuration for the pwc-paper-list skill
#
# 用法：复制本文件为同目录下的 pwc-paper-list.env 并填入实际值
#   cp config/pwc-paper-list.env.example config/pwc-paper-list.env
# 或直接运行 install.sh，它会自动复制本模板到 skill 目录下。
#
# 优先级（高 → 低）：真实 shell 环境变量 > 本文件 > 脚本默认值（~/paper-list）。
# 本文件已被 .gitignore 排除。

# ─────────────────────────────────────────────────────────────
# 输出根目录（可选）
# ─────────────────────────────────────────────────────────────
# 抓取的论文列表 txt 写入此目录，文件名 <conf>_<task>.txt 或 <conf>.txt
# 留空 / 不设置则用默认 ~/paper-list
#
# Windows 示例：D:/study/paper-list
# macOS   示例：/Users/you/paper-list
PAPER_LIST_ROOT=""
